@* @model InkCanvas.Models.Post // Replace 'YourNamespace' with the actual namespace of your view model if needed

@{
    ViewData["Title"] = "Upload Form";
}

<h2>Upload Form</h2>

<form asp-action="Upload" method="post" enctype="multipart/form-data">
    <div class="form-group">
        <label for="file">Choose a PNG file:</label>
        <input type="file" name="file" id="file" class="form-control-file">
        <span class="text-danger">@Html.ValidationMessageFor(m => m.File)</span>
    </div>
    <button type="submit" class="btn btn-primary">Upload</button>
</form>
 *@